Status: done
Created: 2026-08-04
Updated: 2026-08-04
Depends-On: product scope remains uncommitted and unstaged; no external release authorization

# Prepare People Atlas 0.9.0 Alpha release copy locally

## User authorization

The user explicitly authorized on 2026-08-04 only the missing **local** release
copy and its quality checks. This does **not** authorize a commit, push, tag,
GitHub write, publication, asset upload, or Community Plugins submission.

## Outcome

Repair the pre-existing 0.9.0 community-readiness blocker by adding the missing
English release note and matching changelog copy for the already coherent local
version `0.9.0`. The work makes local quality gates meaningful again; it does
not create or publish a release.

## Governing contract

- `.10x/specs/reproducible-obsidian-release.md`
- `package.json`, `manifest.json`, and `versions.json` already agree on `0.9.0`
  with minimum Obsidian `1.13.0`; do not change them.
- The target remains an Alpha candidate titled `People Atlas 0.9.0 (Alpha)` if
  a separately authorized GitHub prerelease is ever performed.

## Scope

1. Add only `.github/release-notes/0.9.0.md` and a top-level `0.9.0` section in
   `CHANGELOG.md`.
2. Describe only the reviewed local product changes:
   - presentation-first person dossiers: safe name-only folders by default,
     collision-only UUID-derived suffixes, and the fresh-vault/no-migration
     boundary;
   - direct canonical `Open note` graph action and Reading View `Add
     relationship` action, each write-free until an explicit modal Save;
   - the simplified daily Settings surface, without changing stored settings
     semantics.
3. Preserve the established English Alpha warning, Obsidian 1.13 compatibility,
   Community Plugins distinction, and exactly the three release assets:
   `main.js`, `manifest.json`, and `styles.css`.
4. Use existing release/community tests and one current Node-24 quality gate.

## Non-goals

- Product code, tests, dependencies, lockfile, workflows, minAppVersion,
  version fields, release assets, vault data, or plugin settings.
- Commit, push, tag, GitHub release/edit/upload, remote configuration, or any
  public release action.
- Any release note for an unreviewed product behavior or a migration claim.

## Acceptance criteria

- [x] `CHANGELOG.md` and `.github/release-notes/0.9.0.md` are English,
      internally consistent, Alpha-labelled, and describe only reviewed scope.
- [x] The release note states Obsidian `1.13.0` or newer, distinguishes a future
      GitHub prerelease from Community Plugins submission, and names exactly
      `main.js`, `manifest.json`, and `styles.css` as assets.
- [x] Version metadata, dependencies, lockfile, workflows, product code, tests,
      generated artifacts, staging, commit history, and remote state are not
      changed by this ticket.
- [x] Targeted release/community checks, an independent read-only review, and
      one current Node-24 `check`/build/reproducibility gate are green.
- [x] No external side effect occurs.

## Verification sequence

1. Validate release-copy content and targeted release/community contracts.
2. Obtain an independent read-only review of release-copy scope; any true
   finding blocks the gate.
3. Run exactly one current full Node-24 gate after all source/test/release-copy
   content is stable: `npm run check`, `npm run build`,
   `npm run verify:reproducible`, and `git diff --check`.
4. Record evidence. Leave the ticket active/done according to actual local
   verification; publication remains separately unauthorized.

## Blockers

None. The independent release-copy rereview and one current shared quality gate
passed; publication remains explicitly unauthorized.

## Journal

- 2026-08-04: The current full quality-gate attempt stopped at the pre-existing
  missing `.github/release-notes/0.9.0.md`: Node tests were 49/50 files and
  933/934 tests green. `HEAD` already has 0.9.0 in package/manifest/versions
  but release notes only through 0.8.0. No production, version, release, GitHub,
  or vault write was made while diagnosing the failure.
- 2026-08-04: Under the user's bounded local authorization, added only the
  English 0.9.0 Alpha changelog section and `.github/release-notes/0.9.0.md`.
  Both cover only presentation-first naming, canonical context actions, the
  reduced Settings surface, fresh-vault/no-migration boundary, compatibility,
  Community Plugins distinction, and the three normal assets. No version,
  product, test, dependency, lockfile, workflow, generated artifact, staging,
  commit, push, tag, remote, or vault change was made.
- 2026-08-04: Read-back confirms the exact 0.9.0 Alpha copy in both files.
  Under `/home/nms/.local/node24/bin/node` (`v24.18.1`, npm `11.16.0`), the
  focused community readiness test is green at **3/3** and
  `npm run test:release-contract` is green at **19/19**. The pre-existing
  feature-code diff remains a separately tracked context ticket; it was not
  created or altered by this release-copy task. A read-only release-copy review
  is now pending before the one current full gate.
- 2026-08-04: De eerste onafhankelijke release-copyreview was terecht **FAIL**:
  uitsluitend `CHANGELOG.md:3` miste het contractuele Alpha-label. De minimale
  releasecopy-remediation veranderde alleen de kop naar `## 0.9.0 (Alpha)`;
  er zijn geen product-, metadata-, dependency-, workflow- of externe acties.
  Daarna zijn community readiness (**3/3**), release-contract (**19/19**) en
  `git diff --check` opnieuw groen. Een korte onafhankelijke rereview loopt;
  pas na PASS volgt precies één actuele volledige gate.
- 2026-08-04: De korte onafhankelijke rereview was **PASS** zonder blocker: de
  enige eerdere finding (`CHANGELOG.md:3`) is met uitsluitend `(Alpha)` hersteld;
  de reviewer bevestigde verder scope, compatibiliteit, drie assets en het
  ontbreken van publicatieautorisatie. Daarna passeerde precies één actuele
  Node-24-gate: `npm run check` met **50/50 nodefiles, 934/934 node tests**,
  **10/10 browserfiles, 130/130 browser tests**, **8/8 integrationfiles,
  28/28 integration tests** en **3/3 browser-matrixfiles, 6/6 matrix tests**;
  production build, community check en release contract waren groen. De
  afzonderlijke build was groen; reproduceerbaarheid gaf tweemaal SHA-256
  `a952761b1fb96d2180ca9c8173dc08c27db87ab1ba1f8b031a87202f21bd3c29`.
  `npm run release:contract -- --tag 0.9.0` bevestigde `main.js`
  **363400/409600 bytes** met exact de drie assets; `git diff --check` was groen.
  Geen staged paden, `main.js` bleef ignored, `main.js.map` bleef afwezig, en
  geen commit/push/tag/GitHub/vault/publicatieactie is uitgevoerd.
- 2026-08-04: Deze lokale 0.9.0-copyvoorbereiding blijft afgerond binnen haar
  expliciete niet-publicerende scope. Een latere read-only remoteinspectie wees
  uit dat `0.9.0` al een geannoteerde publieke tag en een assetloze mislukte
  Alpha-prerelease heeft; de gebruiker koos expliciet de veilige, niet-mutabele
  opvolger `0.10.0`. De externe release verloopt daarom uitsluitend via
  `.10x/tickets/2026-08-04-release-0.10.0-alpha.md`.
